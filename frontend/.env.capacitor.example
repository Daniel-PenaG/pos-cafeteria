# Vite carga este archivo con: npm run build:android  (--mode capacitor)
# Copia a .env.capacitor.local y pon la URL del backend (NO la URL de Vercel).
# Ejemplo si el frontend está en pos-cafeteria-brown.vercel.app:

VITE_API_URL=https://pos-cafeteria-api.onrender.com
